# CI Notes for kotlin-expense-tracker

State: Buildable Android project with app module.

CI behavior:
- Build using Gradle wrapper:
  - (cd kotlin-expense-tracker && ./gradlew assembleDebug)

Notes:
- Requires JDK 17 and Android SDK.
- Parity docs: kavia-docs/expense-tracker-react-spec-for-android-parity.md
