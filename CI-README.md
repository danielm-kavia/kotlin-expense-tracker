Run Gradle from this directory:
- cd kotlin-expense-tracker
- ./gradlew tasks
- ./gradlew assembleDebug

If executing from repository root, use subshell:
- (cd kotlin-expense-tracker && ./gradlew tasks)
